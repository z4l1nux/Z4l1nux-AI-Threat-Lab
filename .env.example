# ==================================================
# CONFIGURAÇÃO DE VARIÁVEIS DE AMBIENTE
# ==================================================
# IMPORTANTE: Copie este arquivo para .env e configure com seus valores reais
# NUNCA commite o arquivo .env no Git!
# ==================================================

# ==================== NEO4J ====================
# URI de conexão do Neo4j (obrigatório)
# Exemplo para local: bolt://localhost:7687
# Exemplo para Neo4j Aura: neo4j+s://xxxxx.databases.neo4j.io
NEO4J_URI=bolt://localhost:7687

# Usuário do Neo4j (obrigatório)
NEO4J_USER=neo4j

# Senha do Neo4j (obrigatório)
# ⚠️ NUNCA use senhas fracas em produção!
# ⚠️ NUNCA commite este arquivo com credenciais reais!
NEO4J_PASSWORD=sua_senha_segura_aqui

# ==================== GEMINI AI ====================
# Chave da API do Google Gemini (obrigatório)
# Obtenha em: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=sua_chave_api_gemini_aqui

# Modelo de embedding (opcional, padrão: gemini-embedding-001)
EMBEDDING_MODEL=gemini-embedding-001

# ==================== BACKEND ====================
# Porta do servidor backend (opcional, padrão: 3001)
BACKEND_PORT=3001

# URL do frontend para CORS (opcional, padrão: http://localhost:5173)
FRONTEND_URL=http://localhost:5173

# Tamanho máximo de arquivo em bytes (opcional, padrão: 10485760 = 10MB)
MAX_FILE_SIZE=10485760

# ==================== FRONTEND ====================
# URL do backend (usado pelo Vite)
VITE_BACKEND_URL=http://localhost:3001

# ==================================================
# INSTRUÇÕES DE SEGURANÇA
# ==================================================
# 1. Copie este arquivo: cp .env.example .env
# 2. Configure suas credenciais reais no arquivo .env
# 3. Verifique se .env está no .gitignore
# 4. Use senhas fortes (mínimo 16 caracteres, com letras, números e símbolos)
# 5. Em produção, use serviços de gerenciamento de secrets (AWS Secrets Manager, Azure Key Vault, etc.)
# 6. Rotacione suas credenciais regularmente
# 7. NUNCA exponha credenciais em logs ou mensagens de erro
# ==================================================
